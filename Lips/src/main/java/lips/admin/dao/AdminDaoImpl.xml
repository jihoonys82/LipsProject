<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

 <mapper namespace="lips.admin.dao.AdminDao">
 	<insert id="inNotice">
 	
 	</insert>
 	
 	<select id="selUserInfo" parameterType="lips.userinfo.dto.User" resultType="lips.userinfo.dto.User">
 		SELECT 
 			USERID,
 			EMAIL,
 			PW,
 			CREATEDATE,
 			NICK,
 			BLOCKED,
 			USERLEVEL,
 			PHONE,
 			BIRTH
 		FROM USERINFO
 	</select>

 </mapper>  